<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GeolocationSample.MainPage"
             Title="CrossPlatformLibrary.Geolocation">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <Button Grid.Row="0"
            x:Name="GetGPSButton"
            Text="Get GPS location"
            Clicked="OnButtonGetGPSLocationClicked"
            VerticalOptions="Start"
            HorizontalOptions="StartAndExpand"/>

   <Label Grid.Row="1" x:Name="LocationOutput" Text="No position retrieved"/>
  </Grid>
</ContentPage>